BACKEND SERVER INSTRUCTIONS

1. Open a new Terminal.
2. Go to the server folder:
   cd server

3. Install dependencies:
   npm install

4. Run the server:
   node index.js

Note: Make sure your MongoDB is running locally at mongodb://localhost:27017 or update the MONGODB_URI in index.js.
